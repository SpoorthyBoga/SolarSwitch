<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Solar Installation in India</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #f9f9f9; }
    .container { margin-top: 30px; }
    .result-box { background: #fff; padding: 20px; border-radius: 10px; box-shadow: 0 0 10px rgba(0,0,0,0.1); }
    .footer {
            text-align: center;
            padding: 30px 0;
            font-size: 14px;
            color: #aaa;
        }

  </style>
</head>
<body>
    <header class="border-bottom py-3">
    <div class="container d-flex justify-content-between align-items-center">
      <div class="d-flex align-items-center gap-2">
        <i data-lucide="sun" class="text-warning"></i>
        <h1 class="h4 fw-bold mb-0">🔆 SolarSwitch</h1>
      </div>
      <nav class="d-none d-md-flex gap-3">
        <a href="/estimate" class="text-decoration-none text-muted">Estimator</a>
        <a href="/ml-analysis" class="text-decoration-none text-muted">ML Analysis</a>
        <a href="/incentives" class="text-decoration-none text-muted">Incentives</a>
        <a href="/about" class="text-decoration-none text-muted">About</a>
      </nav>
      <a href="/estimate" class="btn btn-primary">Get Started</a>
    </div>
  </header>
  <div class="container">
    <h2 class="text-center mb-4">Solar Panel Installation Guide & Payback Estimator (India)</h2>

    <div class="card mb-4">
      <div class="card-body">
        <h5>Requirements for Solar Panel Installation in India:</h5>
        <ul>
          <li>Open rooftop or land space with sufficient sunlight (ideally south-facing)</li>
          <li>Approval from local DISCOM for grid-connected systems</li>
          <li>Empanelled vendor for installation (MNRE approved)</li>
          <li>Net metering arrangement if planning grid-tied setup</li>
          <li>Inverter, battery (optional), structure, wiring, and maintenance plan</li>
        </ul>

        <h5>Steps to Install Solar:</h5>
        <ol>
          <li>Register on MNRE or DISCOM portal (e.g., <a href="https://pmsuryaghar.gov.in" target="_blank">PM Surya Ghar</a>)</li>
          <li>Choose MNRE-approved vendor</li>
          <li>Site survey and quotation</li>
          <li>Installation & inspection by DISCOM</li>
          <li>Subsidy claim and grid synchronization</li>
        </ol>
      </div>
    </div>

    <div class="card mb-4">
      <div class="card-body">
        <h4 class="mb-3">Solar Payback & Savings Calculator</h4>
        <form id="solarForm">
          <div class="row">
            <div class="col-md-4">
              <label for="area" class="form-label">Available Rooftop Area (sq.ft)</label>
              <input type="number" class="form-control" id="area" required>
            </div>
            <div class="col-md-4">
              <label for="rate" class="form-label">Electricity Rate (₹/unit)</label>
              <input type="number" step="0.1" class="form-control" id="rate" value="7" required>
            </div>
            <div class="col-md-4">
              <label for="cost" class="form-label">Installation Cost (₹/kW)</label>
              <input type="number" class="form-control" id="cost" value="50000" required>
            </div>
          </div>
          <div class="mt-3">
            <button type="submit" class="btn btn-primary">Calculate</button>
          </div>
        </form>
        <div id="results" class="result-box mt-4" style="display: none;">
          <h5>Estimates:</h5>
          <p><strong>System Size:</strong> <span id="kwSize"></span> kW</p>
          <p><strong>Energy Generated:</strong> <span id="energy"></span> kWh/month</p>
          <p><strong>Monthly Savings:</strong> ₹<span id="savings"></span></p>
          <p><strong>Total Cost:</strong> ₹<span id="totalCost"></span></p>
          <p><strong>Payback Period:</strong> <span id="payback"></span> years</p>
        </div>
      </div>
    </div>
  </div>
   <div class="footer">
        &copy; 2025 SolarSwitch. All rights reserved.
    </div>

  <script>
    document.getElementById("solarForm").addEventListener("submit", function (e) {
      e.preventDefault();

      const area = parseFloat(document.getElementById("area").value);
      const rate = parseFloat(document.getElementById("rate").value);
      const costPerKW = parseFloat(document.getElementById("cost").value);

      const efficiency = 0.8; // panel efficiency factor
      const wattPerSqFt = 15; // 15 watts per sq.ft.
      const totalKW = (area * wattPerSqFt / 1000) * efficiency;
      const monthlyGen = totalKW * 120; // 120 kWh/month/kW in India approx.
      const savings = monthlyGen * rate;
      const totalCost = totalKW * costPerKW;
      const payback = (totalCost / (savings * 12)).toFixed(1);

      document.getElementById("kwSize").textContent = totalKW.toFixed(2);
      document.getElementById("energy").textContent = Math.round(monthlyGen);
      document.getElementById("savings").textContent = Math.round(savings);
      document.getElementById("totalCost").textContent = Math.round(totalCost);
      document.getElementById("payback").textContent = payback;

      document.getElementById("results").style.display = "block";
    });
  </script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
